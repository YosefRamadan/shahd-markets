# ---------------------------------------------------------------------------
# Shahd Markets — environment configuration
# Copy to .env (local) or set these in your hosting provider's dashboard.
# NEVER commit real secret values. Only the publishable key is safe in a client.
# ---------------------------------------------------------------------------

# --- Client (exposed to the browser by Vite) -------------------------------
VITE_SUPABASE_URL="https://nfzcpxdwbswkigqveocw.supabase.co"
VITE_SUPABASE_PUBLISHABLE_KEY="sb_publishable_MLVg0Zz2oAPixfPyPW1Q7w_Kn2pM-w2"
VITE_SUPABASE_PROJECT_ID="nfzcpxdwbswkigqveocw"

# --- Server (SSR + server functions; never exposed to the browser) ---------
SUPABASE_URL="https://nfzcpxdwbswkigqveocw.supabase.co"
SUPABASE_PUBLISHABLE_KEY="sb_publishable_MLVg0Zz2oAPixfPyPW1Q7w_Kn2pM-w2"
SUPABASE_PROJECT_ID="nfzcpxdwbswkigqveocw"

# SECRET — service role key. Required by server functions that bypass RLS
# (guest carts, guest orders, admin operations, invoices).
# Get it from: Supabase Dashboard > Project Settings > API > service_role.
# Set it as a secret env var in your host. Do NOT commit it.
SUPABASE_SERVICE_ROLE_KEY=""
